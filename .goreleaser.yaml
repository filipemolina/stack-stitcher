version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: stitch
    binary: stitch
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    # The same stamp the Makefile applies, so a released binary and a local
    # `make build` answer --version the same way. .Version is the tag with
    # the leading v stripped, so put it back.
    ldflags:
      - -s -w -X github.com/filipemolina/stack-stitcher/src/constants.version=v{{ .Version }}

# No Windows build: the app shells out to `docker compose` and hands the
# terminal to $EDITOR, neither of which has been tried there.

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md
      - LICENSE

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^Merge branch"
      - "^Pin the phase"

release:
  draft: true
  # Drafted rather than published: the first releases want a human to read
  # the notes before they go out.
  prerelease: auto
